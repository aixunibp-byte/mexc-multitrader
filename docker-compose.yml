services:
  mexc-multitrader:
    build: .
    container_name: mexc-multitrader
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/data
